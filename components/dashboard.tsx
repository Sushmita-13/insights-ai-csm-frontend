import React from 'react';

export const Sidebar = () => (
  // We keep the width (w-64) but remove all content inside
  <nav className="w-64 bg-[#0f172a] min-h-screen hidden lg:flex flex-col">
    {/* Content Removed */}
  </nav>
);